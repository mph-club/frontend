import React from 'react';
import { StyledInputTextField } from './styles';

const InputTextField = (props) => {
    return (
        <StyledInputTextField {...props}/>
    );
};

export default InputTextField